APIとはなんですか？
プログラムからアプリケーションを使えるようにした窓口 

Ajaxとは
JavaScriptを使って任意のタイミングでサーバーと通信する ことをAjaxと呼びます
GooGle mapがその代表例
Ajax通信ではJSONをいうデータ形式でサーバーとやり取りするのが一般できである。
ページ遷移とは関係のないタイミングでデータ通信が可能である。

JSONのコードはjsとの相互変換が可能である。

通常APIを使用する際はAPIキーというものを発効しそれを使用する。
APIへのアクセス許可証的な役割を持つ

	test
Key:
9db24122ba3499f38d6259e6d6850c0d

Secret:
f9d9f2163f9ad042


$.param() はパラメータを与えると、URL用の文字列へ変換してくれます。

jQueryの $.getJSON() というメソッドは、サーバーにリクエストを送り、返ってきたJSON文字列をオブジェクトに変換してくれます。